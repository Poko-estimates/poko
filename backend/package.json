{
  "name": "@poko/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "supabase start",
    "stop": "supabase stop",
    "status": "supabase status",
    "reset": "supabase db reset",
    "migration": "supabase migration new",
    "gen:types": "mkdir -p ../frontend/lib/supabase && supabase gen types typescript --local --schema public > ../frontend/lib/supabase/database.types.ts",
    "gen:types:linked": "mkdir -p ../frontend/lib/supabase && supabase gen types typescript --linked --schema public > ../frontend/lib/supabase/database.types.ts",
    "test:db": "supabase test db",
    "link": "supabase link",
    "push": "supabase db push"
  },
  "devDependencies": {
    "supabase": "^2.100.1"
  }
}
