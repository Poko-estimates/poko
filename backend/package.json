{
  "name": "@poko/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preinstall": "node -e \"if(!/^pnpm\\//.test(process.env.npm_config_user_agent||'')){console.error('\\n  This repo uses pnpm. Run `pnpm install` from the repo root.\\n  Adding a package: pnpm add --filter @poko/backend <pkg>\\n');process.exit(1)}\"",
    "start": "supabase start",
    "stop": "supabase stop",
    "status": "supabase status",
    "reset": "supabase db reset",
    "migration": "supabase migration new",
    "gen:types": "supabase gen types typescript --local > types/database.ts",
    "link": "supabase link",
    "push": "supabase db push"
  },
  "devDependencies": {
    "supabase": "^2.100.1"
  }
}
